import React, { Component } from "react";

export class Header extends Component {
  render = () => {
    return (
      <nav className="navbar navbar-dark  bg-info p-3">
        <h4 className="text-white font-weight-bold text-center">SOCKET TEST</h4>
      </nav>
    );
  };
}
