import React, { Component } from "react";

export class Header extends Component {
  render = () => {
    return (
      <nav className="navbar navbar-dark bg-dark p-3">
        <h4 className="text-white text-center">SOCKET TEST</h4>
      </nav>
    );
  };
}
